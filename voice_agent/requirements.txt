# Core
Flask==3.0.0
Werkzeug==3.0.0
flask-cors==4.0.0
python-dotenv==1.0.0
requests==2.31.0

# AI / ASR (OpenAI Whisper)
openai==1.12.0

# Testing
pytest==7.4.3

# Optional: only needed if using Google Speech-to-Text backend
# google-cloud-speech==2.21.0

# Optional: only needed if using spacy-based NER (not currently used)
# spacy==3.7.2

# Optional: only needed if using pydantic models for validation
# pydantic==2.5.2
